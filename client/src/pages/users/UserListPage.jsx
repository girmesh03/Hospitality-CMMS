import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * User list page placeholder — Phase 06 implements full user management.
 * @returns {JSX.Element}
 */
export default function UserListPage() {
  return <PagePlaceholder title="Users" />;
}
