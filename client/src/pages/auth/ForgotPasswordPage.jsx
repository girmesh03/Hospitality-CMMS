import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Forgot password page placeholder — Phase 04 implements full auth flow.
 * @returns {JSX.Element}
 */
export default function ForgotPasswordPage() {
  return <PagePlaceholder title="Forgot Password" />;
}
