import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Reset password page placeholder — Phase 04 implements full auth flow.
 * @returns {JSX.Element}
 */
export default function ResetPasswordPage() {
  return <PagePlaceholder title="Reset Password" />;
}
