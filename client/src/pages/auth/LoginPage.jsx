import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Login page placeholder — Phase 04 implements full auth flow.
 * @returns {JSX.Element}
 */
export default function LoginPage() {
  return <PagePlaceholder title="Login" />;
}
