import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Work order list page placeholder — Phase 09 implements full work orders.
 * @returns {JSX.Element}
 */
export default function WorkOrderListPage() {
  return <PagePlaceholder title="Work Orders" />;
}
