import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Notification center page placeholder — Phase 14 implements full notifications.
 * @returns {JSX.Element}
 */
export default function NotificationCenterPage() {
  return <PagePlaceholder title="Notifications" />;
}
