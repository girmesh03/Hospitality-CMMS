import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Property list page placeholder — Phase 05 implements full properties.
 * @returns {JSX.Element}
 */
export default function PropertyListPage() {
  return <PagePlaceholder title="Properties" />;
}
