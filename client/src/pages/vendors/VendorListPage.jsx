import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Vendor list page placeholder — Phase 13 implements full vendors.
 * @returns {JSX.Element}
 */
export default function VendorListPage() {
  return <PagePlaceholder title="Vendors" />;
}
