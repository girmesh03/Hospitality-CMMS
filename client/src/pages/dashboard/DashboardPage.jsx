import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Dashboard page placeholder — Phase 15 implements full dashboard.
 * @returns {JSX.Element}
 */
export default function DashboardPage() {
  return <PagePlaceholder title="Dashboard" />;
}
