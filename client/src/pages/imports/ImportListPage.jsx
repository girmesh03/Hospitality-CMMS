import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Imports list page placeholder — Phase 16 implements full imports.
 * @returns {JSX.Element}
 */
export default function ImportListPage() {
  return <PagePlaceholder title="Imports" />;
}
