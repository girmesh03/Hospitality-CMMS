import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Role list page placeholder — Phase 06 implements full role management.
 * @returns {JSX.Element}
 */
export default function RoleListPage() {
  return <PagePlaceholder title="Roles" />;
}
