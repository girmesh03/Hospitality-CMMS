import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Vendor portal page placeholder — Phase 13 implements full vendor portal.
 * @returns {JSX.Element}
 */
export default function VendorPortalPage() {
  return <PagePlaceholder title="Vendor Portal" />;
}
