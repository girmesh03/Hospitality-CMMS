import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Request portal page placeholder — Phase 08 implements full portal.
 * @returns {JSX.Element}
 */
export default function RequestPortalPage() {
  return <PagePlaceholder title="Request Portal" />;
}
