import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Work request list page placeholder — Phase 08 implements full requests.
 * @returns {JSX.Element}
 */
export default function RequestListPage() {
  return <PagePlaceholder title="Requests" />;
}
