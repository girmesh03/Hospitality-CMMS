import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Inventory list page placeholder — Phase 12 implements full inventory.
 * @returns {JSX.Element}
 */
export default function InventoryListPage() {
  return <PagePlaceholder title="Inventory" />;
}
