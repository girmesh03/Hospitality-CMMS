import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * PM plans list page placeholder — Phase 10 implements full PM.
 * @returns {JSX.Element}
 */
export default function PMListPage() {
  return <PagePlaceholder title="Preventive Maintenance Plans" />;
}
