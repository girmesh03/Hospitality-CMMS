import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Profile settings page placeholder — Phase 04 implements full profile.
 * @returns {JSX.Element}
 */
export default function ProfilePage() {
  return <PagePlaceholder title="Profile Settings" />;
}
