import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Team list page placeholder — Phase 06 implements full team management.
 * @returns {JSX.Element}
 */
export default function TeamListPage() {
  return <PagePlaceholder title="Teams" />;
}
