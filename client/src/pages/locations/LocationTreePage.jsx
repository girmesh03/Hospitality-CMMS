import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Location tree page placeholder — Phase 05 implements full locations.
 * @returns {JSX.Element}
 */
export default function LocationTreePage() {
  return <PagePlaceholder title="Locations" />;
}
