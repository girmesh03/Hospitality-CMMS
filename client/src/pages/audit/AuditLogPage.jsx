import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Audit log page placeholder — Phase 15 implements full audit.
 * @returns {JSX.Element}
 */
export default function AuditLogPage() {
  return <PagePlaceholder title="Audit Log" />;
}
