import PagePlaceholder from "../PagePlaceholder.jsx";

/**
 * Asset list page placeholder — Phase 07 implements full assets.
 * @returns {JSX.Element}
 */
export default function AssetListPage() {
  return <PagePlaceholder title="Assets" />;
}
