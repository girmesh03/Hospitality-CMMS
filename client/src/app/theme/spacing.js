/**
 * Shared spacing and shape tokens for the Hospitality CMMS frontend.
 */

/**
 * Base MUI spacing unit.
 */
export const APP_SPACING = 8;

/**
 * Shared shape tokens.
 */
export const APP_SHAPE = Object.freeze({
  borderRadius: 16,
});

export default {
  APP_SPACING,
  APP_SHAPE,
};
