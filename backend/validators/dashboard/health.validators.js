/**
 * Validator list for the API descriptor endpoint.
 *
 * Empty by design while still preserving the standardized validation pipeline.
 *
 * @type {import("express-validator").ValidationChain[]}
 */
export const getApiDescriptorValidators = [];

/**
 * Validator list for the API health endpoint.
 *
 * Empty by design while still preserving the standardized validation pipeline.
 *
 * @type {import("express-validator").ValidationChain[]}
 */
export const getApiHealthValidators = [];
